num = int (input("Enter a number till you want f series = "))
if num <= 0:
    print("Nothing to print :) ")

else:
    a = 0
    b = 1
    print(a)
if num > 1:
    print (b)
    for i in range (2,num) :
      total = a + b
      a = b 
      b = total
      print (total)

